run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - gosimple
    - unused
    - gofmt
    - misspell

linters-settings:
  errcheck:
    check-type-assertions: true
