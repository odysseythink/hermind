# Hermind Browser Extension

Chrome extension for saving web content to Hermind.

## Development

```bash
cd browser-extension
yarn install
yarn build
```

## Load in Chrome

1. Open `chrome://extensions/`
2. Enable "Developer mode"
3. Click "Load unpacked"
4. Select `browser-extension/dist/`