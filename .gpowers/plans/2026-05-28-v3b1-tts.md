# v3-B1 — TTS (Text-to-Speech) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `gpowers:subagent-driven-development` (recommended) or `gpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `cfg.TTSProvider` 这个空挂的 env 变成一个真实可用的 TTS 子系统:支持 3 个 hosted provider(`elevenlabs` / `openai` / `openai-generic`)+ `native` 占位(前端走 Web Speech API),并暴露 `GET /api/workspace/:slug/tts/:chatId` 端点 Node parity。**估算 5.5h**。

**先决条件**: 无;独立子系统。

**Source spec:** `.gpowers/designs/2026-05-27-v3b-user-facing-design.md` §3。

**Reference Node implementation:**
- `server/utils/TextToSpeech/{elevenLabs,openAi,openAiGeneric}/index.js`
- `server/endpoints/workspaces.js:615` — `GET /workspace/:slug/tts/:chatId`

---

## Pre-task

### 现状

- `internal/config/config.go:23` 已声明 `TTSProvider string` env(default `native`),**零实装**
- 无 `internal/tts/` 包
- 无 TTS endpoint
- WorkspaceChat.Response 字段是 JSON 字符串(`{"text":"...","sources":[...]}`),TTS handler 需要 unmarshal 后取 `text` 字段

### 新增 surface

```
backend/internal/tts/
├── doc.go
├── provider.go          # Provider interface + Synthesis 类型
├── factory.go           # NewProvider(cfg, settings)
├── factory_test.go
├── elevenlabs.go
├── elevenlabs_test.go
├── openai.go
├── openai_test.go
├── openai_generic.go
├── openai_generic_test.go
└── native.go            # NoopProvider stub

backend/internal/handlers/
├── tts.go               # GET /api/workspace/:slug/tts/:chatId
└── tts_test.go

backend/internal/config/config.go  # +8 env 字段
backend/cmd/server/main.go          # wire
```

### 接口

```go
type Synthesis struct {
    Audio       []byte
    ContentType string  // "audio/mpeg" 等
}

type Provider interface {
    Synthesize(ctx context.Context, text string) (*Synthesis, error)
    Available() bool
    Name() string
}
```

### TDD discipline

每 task 一 commit。failing test → impl → green → suite green → commit。

---

## Task 1: Config + 包骨架 + native stub

**Files:**
- `backend/internal/config/config.go` (MODIFY)
- `backend/internal/tts/doc.go` (NEW)
- `backend/internal/tts/provider.go` (NEW)
- `backend/internal/tts/native.go` (NEW)

**Tests:** none yet(declarative)

### Steps

- [ ] 加 8 个 env 到 Config:
  ```go
  // === ElevenLabs TTS ===
  ElevenLabsAPIKey       string `env:"ELEVENLABS_API_KEY"`
  ElevenLabsVoiceID      string `env:"ELEVENLABS_VOICE_ID" envDefault:"21m00Tcm4TlvDq8ikWAM"`
  ElevenLabsModel        string `env:"ELEVENLABS_MODEL" envDefault:"eleven_monolingual_v1"`
  // === OpenAI TTS ===
  OpenAITTSModel         string `env:"OPEN_AI_TTS_MODEL" envDefault:"tts-1"`
  OpenAITTSVoice         string `env:"OPEN_AI_TTS_VOICE" envDefault:"alloy"`
  // === OpenAI Generic TTS ===
  TTSOpenAICompatKey     string `env:"TTS_OPEN_AI_COMPATIBLE_KEY"`
  TTSOpenAICompatEndpoint string `env:"TTS_OPEN_AI_COMPATIBLE_ENDPOINT"`
  TTSOpenAICompatModel   string `env:"TTS_OPEN_AI_COMPATIBLE_MODEL" envDefault:"tts-1"`
  TTSOpenAICompatVoice   string `env:"TTS_OPEN_AI_COMPATIBLE_VOICE" envDefault:"alloy"`
  ```

- [ ] 写 `doc.go`:
  ```go
  // Package tts provides text-to-speech synthesis for chat responses.
  // Supports 3 hosted providers (elevenlabs / openai / openai-generic)
  // plus a "native" stub for the browser's Web Speech API.
  package tts
  ```

- [ ] 写 `provider.go`:
  ```go
  type Synthesis struct {
      Audio       []byte
      ContentType string
  }
  type Provider interface {
      Synthesize(ctx context.Context, text string) (*Synthesis, error)
      Available() bool
      Name() string
  }
  ```

- [ ] 写 `native.go`:
  ```go
  type nativeProvider struct{}
  func NewNativeProvider() Provider { return &nativeProvider{} }
  func (n *nativeProvider) Name() string { return "native" }
  func (n *nativeProvider) Available() bool { return true }
  func (n *nativeProvider) Synthesize(ctx context.Context, text string) (*Synthesis, error) {
      return nil, fmt.Errorf("native TTS is handled by the browser; server-side synthesis not available")
  }
  ```

- [ ] `go build ./...` + `go vet ./...` 干净。

### Acceptance

- 8 个新 env 解析正常,defaults 合理
- `Provider` 接口 + `Synthesis` 类型就位
- `nativeProvider` 返回明确的 error message

### Commit

`feat(tts): bootstrap package + native stub + 8 env knobs`

---

## Task 2: ElevenLabs provider

**Files:**
- `backend/internal/tts/elevenlabs.go` (NEW)
- `backend/internal/tts/elevenlabs_test.go` (NEW)

**Tests:**
- `TestElevenLabs_HappyPath_ReturnsMPEG`
- `TestElevenLabs_NoAPIKey_NotAvailable`
- `TestElevenLabs_HTTPError_Returns5xx`
- `TestElevenLabs_RequestBody_ContainsTextAndModel`
- `TestElevenLabs_RequestHeaders_XAPIKey`

### Steps

- [ ] 写失败测试,httptest mock:
  ```go
  func TestElevenLabs_HappyPath_ReturnsMPEG(t *testing.T) {
      srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
          require.Equal(t, "test-key", r.Header.Get("xi-api-key"))
          require.Contains(t, r.URL.Path, "/v1/text-to-speech/voice-abc")
          var body map[string]any
          json.NewDecoder(r.Body).Decode(&body)
          require.Equal(t, "hello world", body["text"])
          w.Header().Set("Content-Type", "audio/mpeg")
          w.Write([]byte{0xFF, 0xFB, 0x90, 0x00})  // fake MP3 header
      }))
      defer srv.Close()

      cfg := &config.Config{
          ElevenLabsAPIKey: "test-key",
          ElevenLabsVoiceID: "voice-abc",
          ElevenLabsModel: "eleven_monolingual_v1",
      }
      p := tts.NewElevenLabsProvider(cfg, nil)
      tts.SetTestElevenLabsBaseURL(srv.URL)
      defer tts.SetTestElevenLabsBaseURL("")

      out, err := p.Synthesize(context.Background(), "hello world")
      require.NoError(t, err)
      require.Equal(t, "audio/mpeg", out.ContentType)
      require.NotEmpty(t, out.Audio)
  }
  ```

- [ ] 实装 `elevenlabs.go`:
  ```go
  package tts

  import (
      "bytes"
      "context"
      "encoding/json"
      "fmt"
      "io"
      "net/http"
      "time"

      "github.com/odysseythink/hermind/backend/internal/config"
  )

  var testElevenLabsBaseURL string
  func SetTestElevenLabsBaseURL(u string) { testElevenLabsBaseURL = u }

  type elevenLabsProvider struct {
      apiKey, voiceID, model string
      http   *http.Client
  }

  func NewElevenLabsProvider(cfg *config.Config, settings map[string]string) Provider {
      return &elevenLabsProvider{
          apiKey:  pick("ElevenLabsApiKey", settings, cfg.ElevenLabsAPIKey),
          voiceID: pick("ElevenLabsVoiceID", settings, cfg.ElevenLabsVoiceID),
          model:   pick("ElevenLabsModel", settings, cfg.ElevenLabsModel),
          http:    &http.Client{Timeout: 30 * time.Second},
      }
  }
  func (p *elevenLabsProvider) Name() string     { return "elevenlabs" }
  func (p *elevenLabsProvider) Available() bool  { return p.apiKey != "" && p.voiceID != "" }

  func (p *elevenLabsProvider) Synthesize(ctx context.Context, text string) (*Synthesis, error) {
      base := "https://api.elevenlabs.io"
      if testElevenLabsBaseURL != "" { base = testElevenLabsBaseURL }
      url := fmt.Sprintf("%s/v1/text-to-speech/%s", base, p.voiceID)
      body, _ := json.Marshal(map[string]any{
          "text": text, "model_id": p.model,
          "voice_settings": map[string]float64{"stability": 0.5, "similarity_boost": 0.5},
      })
      req, _ := http.NewRequestWithContext(ctx, "POST", url, bytes.NewReader(body))
      req.Header.Set("xi-api-key", p.apiKey)
      req.Header.Set("Content-Type", "application/json")
      resp, err := p.http.Do(req)
      if err != nil { return nil, fmt.Errorf("elevenlabs: %w", err) }
      defer resp.Body.Close()
      if resp.StatusCode >= 400 {
          msg, _ := io.ReadAll(io.LimitReader(resp.Body, 4096))
          return nil, fmt.Errorf("elevenlabs HTTP %d: %s", resp.StatusCode, msg)
      }
      audio, err := io.ReadAll(io.LimitReader(resp.Body, 50<<20))  // 50 MiB cap
      if err != nil { return nil, fmt.Errorf("elevenlabs read: %w", err) }
      return &Synthesis{Audio: audio, ContentType: "audio/mpeg"}, nil
  }

  // pick utility (also used by openai/openai_generic providers)
  func pick(key string, settings map[string]string, fallback string) string {
      if v, ok := settings[key]; ok && v != "" { return v }
      return fallback
  }
  ```

- [ ] 5 个测试全过。

### Acceptance

- 5 个测试通过
- 50 MiB audio cap 防止 OOM
- `xi-api-key` header 正确传

### Commit

`feat(tts): ElevenLabs provider`

---

## Task 3: OpenAI + OpenAI Generic provider

**Files:**
- `backend/internal/tts/openai.go` (NEW)
- `backend/internal/tts/openai_test.go` (NEW)
- `backend/internal/tts/openai_generic.go` (NEW)
- `backend/internal/tts/openai_generic_test.go` (NEW)

**Tests:**
- `TestOpenAITTS_HappyPath_ReturnsMPEG`
- `TestOpenAITTS_NoAPIKey_NotAvailable`
- `TestOpenAITTS_RequestShape` (Authorization Bearer + model + input + voice)
- `TestOpenAIGeneric_CustomEndpoint_OverridesDefault`
- `TestOpenAIGeneric_NoEndpoint_NotAvailable`

### Steps

- [ ] 实装 `openai.go`(用 `OPEN_AI_KEY` config 字段,与 LLM 同源):
  ```go
  package tts

  // (same shape as elevenLabs but POST /v1/audio/speech, Bearer auth)
  type openAIProvider struct { /* apiKey, model, voice */ }

  func NewOpenAIProvider(cfg *config.Config, settings map[string]string) Provider {
      return &openAIProvider{
          apiKey: pick("OpenAiKey", settings, cfg.OpenAiKey),
          model:  pick("OpenAITTSModel", settings, cfg.OpenAITTSModel),
          voice:  pick("OpenAITTSVoice", settings, cfg.OpenAITTSVoice),
          http:   &http.Client{Timeout: 30 * time.Second},
      }
  }

  func (p *openAIProvider) Synthesize(ctx context.Context, text string) (*Synthesis, error) {
      url := "https://api.openai.com/v1/audio/speech"
      if testOpenAITTSBaseURL != "" { url = testOpenAITTSBaseURL + "/v1/audio/speech" }
      body, _ := json.Marshal(map[string]any{
          "model": p.model, "input": text, "voice": p.voice,
      })
      req, _ := http.NewRequestWithContext(ctx, "POST", url, bytes.NewReader(body))
      req.Header.Set("Authorization", "Bearer " + p.apiKey)
      req.Header.Set("Content-Type", "application/json")
      // ... (same as elevenLabs response handling)
  }
  ```

- [ ] 实装 `openai_generic.go`(完全同款 OpenAI 但 BaseURL 来自 `TTSOpenAICompatEndpoint`):
  ```go
  type openAIGenericProvider struct { /* apiKey, endpoint, model, voice */ }

  func NewOpenAIGenericProvider(cfg *config.Config, settings map[string]string) Provider { /* ... */ }
  func (p *openAIGenericProvider) Available() bool { return p.endpoint != "" }
  // Synthesize 直接拼 endpoint + "/audio/speech"(由用户配置完整 base URL)
  ```

- [ ] 写 5 个测试。

### Acceptance

- 5 个测试通过
- OpenAI 走默认 `api.openai.com`;Generic 走 `cfg.TTSOpenAICompatEndpoint`
- 缺 endpoint 时 Generic.Available() == false

### Commit

`feat(tts): OpenAI + OpenAI Generic providers`

---

## Task 4: Factory + 5 case 路由

**Files:**
- `backend/internal/tts/factory.go` (NEW)
- `backend/internal/tts/factory_test.go` (NEW)

**Tests:**
- `TestFactory_ElevenLabs_BuildsProvider`
- `TestFactory_OpenAI_BuildsProvider`
- `TestFactory_OpenAIGeneric_BuildsProvider`
- `TestFactory_Native_DefaultForUnknown`
- `TestFactory_EmptyConfig_FallsBackToNative`
- `TestFactory_SettingsOverridesCfg`

### Steps

- [ ] 实装 `factory.go`:
  ```go
  func NewProvider(cfg *config.Config, settings map[string]string) Provider {
      name := strings.ToLower(strings.TrimSpace(pick("TTSProvider", settings, cfg.TTSProvider)))
      switch name {
      case "elevenlabs":
          return NewElevenLabsProvider(cfg, settings)
      case "openai":
          return NewOpenAIProvider(cfg, settings)
      case "openai-generic":
          return NewOpenAIGenericProvider(cfg, settings)
      default:
          return NewNativeProvider()
      }
  }
  ```

- [ ] 6 个工厂测试。

### Acceptance

- 6 个测试通过
- `settings["TTSProvider"]` 优先于 `cfg.TTSProvider`
- 未知 provider fall back 到 native

### Commit

`feat(tts): factory routing 4 providers`

---

## Task 5: HTTP handler + endpoint

**Files:**
- `backend/internal/handlers/tts.go` (NEW)
- `backend/internal/handlers/tts_test.go` (NEW)
- `backend/internal/services/workspace_chat_service.go` 或就近 (MODIFY — 加 GetChatByID if missing)

**Tests:**
- `TestTTSHandler_HappyPath_200WithAudio`
- `TestTTSHandler_ChatNotFound_404`
- `TestTTSHandler_NoAssistantText_422`
- `TestTTSHandler_NativeProvider_ReturnsError`
- `TestTTSHandler_RequiresAuth_401`

### Steps

- [ ] 确保有 `chatSvc.GetChatByID(ctx, id) (*WorkspaceChat, error)`,没有就加:
  ```go
  // services/chat_service.go
  func (s *ChatService) GetChatByID(ctx context.Context, id int) (*models.WorkspaceChat, error) {
      var chat models.WorkspaceChat
      if err := s.db.WithContext(ctx).First(&chat, id).Error; err != nil { return nil, err }
      return &chat, nil
  }
  ```

- [ ] 实装 handler:
  ```go
  // handlers/tts.go
  type TTSHandler struct {
      chatSvc *services.ChatService
      tts     tts.Provider
  }

  func RegisterTTSRoutes(r *gin.RouterGroup, h *TTSHandler, authSvc *services.AuthService) {
      r.GET("/workspace/:slug/tts/:chatId",
          middleware.ValidatedRequest(authSvc),
          h.Synthesize,
      )
  }

  func (h *TTSHandler) Synthesize(c *gin.Context) {
      chatID, err := strconv.Atoi(c.Param("chatId"))
      if err != nil { c.JSON(400, gin.H{"error":"invalid chatId"}); return }
      chat, err := h.chatSvc.GetChatByID(c.Request.Context(), chatID)
      if err != nil { c.JSON(404, gin.H{"error":"chat not found"}); return }

      var resp struct{ Text string `json:"text"` }
      _ = json.Unmarshal([]byte(chat.Response), &resp)
      text := strings.TrimSpace(resp.Text)
      if text == "" { c.JSON(422, gin.H{"error":"no text to synthesize"}); return }

      out, err := h.tts.Synthesize(c.Request.Context(), text)
      if err != nil { c.JSON(500, gin.H{"error":err.Error()}); return }
      c.Data(200, out.ContentType, out.Audio)
  }
  ```

- [ ] 5 个测试,handler 层 + service mock。

### Acceptance

- 5 个测试通过
- `chat.Response` 是 JSON 字符串,正确 unmarshal 取 text
- native provider 时返回 500 + 明确 error message

### Commit

`feat(handlers): TTS endpoint /api/workspace/:slug/tts/:chatId`

---

## Task 6: main.go 接线 + 文档

**Files:**
- `backend/cmd/server/main.go` (MODIFY)
- `.env.example` (MODIFY)

**Tests:** none new(boot smoke)

### Steps

- [ ] main.go 加构造 + 注册:
  ```go
  // 加在 chatSvc 构造后
  settingsForBoot, _ := sysSvc.GetAllSettings(context.Background())
  ttsProvider := tts.NewProvider(cfg, settingsForBoot)
  ttsHandler := handlers.NewTTSHandler(chatSvc, ttsProvider)
  handlers.RegisterTTSRoutes(api, ttsHandler, authSvc)
  ```

- [ ] `.env.example` 加 8 个 TTS env 的注释块。

- [ ] Boot smoke:`go run ./cmd/server`,无错。

### Acceptance

- Boot 干净;`curl /api/workspace/test/tts/1` 返回 401(未鉴权)而非 panic
- 用真 OPEN_AI_KEY 跑 e2e 能听到合成音(integration test,可选)

### Commit

`feat(server): wire TTS provider + endpoint`

---

## Post-PR checklist

- [ ] `go build ./...` 干净
- [ ] `go vet ./...` 干净
- [ ] `go test ./... -race` 100% 绿
- [ ] `.env.example` 含 8 个新 TTS env
- [ ] 文档 `docs/PROVIDERS.md` 或 README 加 TTS 一节(4 个 provider 名 + 配置 key)

## Risk notes

| Risk | Mitigation |
|---|---|
| 50 MiB audio cap 错误音频被截 | 30s 文本约 50KB 音频,远低于 cap;cap 是 OOM 保护 |
| ElevenLabs voice_id 错误返回 404 → 用户看到模糊 error | error message 透传 elevenlabs response body 头 4096 字节 |
| OpenAI TTS 模型 `tts-1` 不支持中文 → 输出静音 | 用户自己选 `tts-1-hd` 或别的模型;不在 server 限制 |
| WorkspaceChat.Response 不是 JSON(老数据) | unmarshal 失败时直接用 raw string 当 text(fall through) |

## Estimate

| Task | Hours |
|---|---|
| 1. Config + 骨架 + native | 0.5 |
| 2. ElevenLabs | 1.5 |
| 3. OpenAI + Generic | 1.5 |
| 4. Factory | 0.5 |
| 5. Handler + endpoint | 1.0 |
| 6. main.go + docs | 0.5 |
| **Total** | **5.5** (design 估 5.5h ✓) |

—— end of plan
