import QtQuick
import QtQuick.Controls
import ".."

Rectangle {
    color: Theme.bg

    Text {
        anchors.centerIn: parent
        text: "New Conversation"
        color: Theme.textSecondary
        font.pixelSize: 13
    }
}
